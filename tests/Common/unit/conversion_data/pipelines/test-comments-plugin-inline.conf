input {
	udp {
		# inline comment on plugin opener
		# inline comment on a scalar value
		# another scalar inline comment
		# inline comment after an array
		port => 5140
		buffer_size => 65536
		tags => ["udp", "syslog"]
	}
	# inline comment on plugin closer — becomes section-level comment
}
filter {
	mutate {
		# opener comment
		# inline comment on hash opener
		# inline comment on hash pair
		# another hash pair comment
		# inline comment on hash closer
		# inline on array
		# another hash opener with inline
		# pair comment
		# hash closer comment
		add_field => {
			"first" => "value1"
			"second" => "value2"
		}
		remove_field => ["unwanted", "junk"]
		rename => {
			"old_name" => "new_name"
		}
	}
	# plugin closer — section-level
	drop {
		# opener comment on empty plugin
	}
	# closer comment on empty plugin
}
output {
	stdout {
		# output plugin with inline
		# inline on codec line
		codec => rubydebug
	}
	# closer
}
