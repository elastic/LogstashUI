input {
}
filter {
	grok {
		match => {
			"test" => "test"
		}
		pattern_definitions => {
			"1" => "2"
			"test" => "test"
			"asd" => "asf"
		}
		patterns_dir => ["test"]
		tag_on_failure => []
	}
	grok {
		match => {
			"test" => [
				"test",
				"test2"
			]
		}
		pattern_definitions => {
			"test" => "test"
		}
		patterns_dir => "test"
	}
	# test
	# multi
	# row
}
output {
}
