input { pipeline { address => "filter-final" } }

filter {
  ruby {
    code => "
      event.set('[@metadata][PIPELINE-ID][final]', event.to_hash)
    "
  }
}

output {
  pipeline { send_to => "output-block" }
}
