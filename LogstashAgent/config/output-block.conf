input { pipeline { address => "output-block" } }

filter { }

output {
  # No output needed - each filter already sends its own HTTP output
  # This pipeline just acts as a sink for the pipeline-to-pipeline chain
  stdout { codec => rubydebug }
}
